"""Read-only: look at what a run recorded, or at what the browser can name.

    python -m tools.inspect.show_run [runs/x]        a saved run's trail as a story
    python -m tools.inspect.a11y_dump URL            the ARIA snapshot, and which controls rung 1 finds
"""
