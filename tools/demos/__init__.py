"""The two demonstrations, as scripts a reader can run against the demo app.

    python -m tools.demos.b1     B1: a control with no accessible name, found anyway
    python -m tools.demos.b2     B2: one artifact, two institutions, with and without its Overlay

Both are also tests (tests/test_demos.py), so they cannot quietly stop working.
"""
